#include <stdio.h>

int main()
{
    char a[100];
    int i=0,f;
    printf("enter the character");
    scanf("%s",&a);
    for(i=0;i<=a[100];i++)
    {
        if(i!=0)
        {
            f=f+1;
            printf("print the numbers");
        
        }
        else
        {
            return 0;
        }
        
    
    }

    return 0;
}
